# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Regeneratable ELO computation caches (large; rebuilt on first run)
cache/elo_*.pkl

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code local config
.claude/

# Streamlit secrets
.streamlit/secrets.toml

# Retrosheet artifacts belong to the wOBA+historical (v2) project, not this
# Statcast-only expected-stats build — never commit them here.
retrosheet_raw/
cache/retro_*.parquet

# Keep the small Statcast parquet caches so the app loads instantly:
# cache/statcast_*.parquet and cache/player_names.parquet ARE committed.
